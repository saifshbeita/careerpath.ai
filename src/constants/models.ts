/** Realtime conversational model used for the live voice interview. */
export const LIVE_MODEL = 'gemini-2.5-flash-native-audio-preview-09-2025';

/** Text-to-speech model used to synthesize the welcome greeting. */
export const TTS_MODEL = 'gemini-2.5-flash-preview-tts';

/** Reasoning model used to generate the final career analysis report. */
export const ANALYSIS_MODEL = 'gemini-2.5-pro';

/** Prebuilt Gemini voice used for all coach speech. */
export const COACH_VOICE = 'Zephyr';
